# Copy to .env.local for local frontend development.
#
# Bare backend origin — do NOT include a /api/v1 suffix. The app appends /api/v1
# in code (see frontend/lib/config.ts). A trailing /api/v1 is now stripped
# automatically, but keeping it bare avoids confusion.
NEXT_PUBLIC_API_URL=http://localhost:8080
