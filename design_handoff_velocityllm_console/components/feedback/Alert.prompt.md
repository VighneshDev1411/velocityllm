Inline banner for page-level notices — empty states, error banners, informational callouts.

```jsx
<Alert icon={<Icon name="zap" size={18}/>} title="No requests yet.">
  Send prompts via the Playground to populate this dashboard.
</Alert>
```
