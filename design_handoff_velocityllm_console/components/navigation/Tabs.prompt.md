Segmented control for switching between a small set of views (time ranges, chart modes).

```jsx
<Tabs tabs={[{value:'24h',label:'24h'},{value:'7d',label:'7d'}]} value={range} onChange={setRange} />
```
