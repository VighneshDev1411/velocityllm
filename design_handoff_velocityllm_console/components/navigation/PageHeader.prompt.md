Standard header for every console page — title, one-line subtitle, and an optional action slot on the right (usually a `Tabs` time-range switcher or a primary `Button`).

```jsx
<PageHeader title="Dashboard" subtitle="Real-time system monitoring & analytics" action={<Tabs .../>} />
```
