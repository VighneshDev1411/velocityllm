A plain text input. Border is transparent-ish by default (uses the raised surface-hover fill) and picks up a periwinkle focus ring on click.

```jsx
<Input placeholder="you@company.com" type="email" />
```

Pair with `Label` above it and, in forms, `Select` for enumerated choices.
