Tinted status pill for tables and lists — request status, worker health, job state.

```jsx
<StatusChip label="Healthy" status="success" />
<StatusChip label="Degraded" status="warning" />
```

Distinct from `Badge`: `StatusChip` colors are tied to semantic state (success/error/warning/info), not brand/variant.
