Small pill label. Use for counts, plan tiers, or short tags (not for live system status — use `StatusChip` for that, it has success/error/warning/info coloring).

```jsx
<Badge variant="secondary">Pro plan</Badge>
```
